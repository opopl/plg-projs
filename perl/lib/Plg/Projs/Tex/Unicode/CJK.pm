
package Plg::Projs::Tex::Unicode::CJK;

use utf8;
use strict;
use warnings;

use constant MAP => map { $_->{char} => '' } (
  #{ char => "\N{<++>}", name => '<++>' },
  #{ char => "\N{<++>}", name => '<++>' },
);

1;
 

