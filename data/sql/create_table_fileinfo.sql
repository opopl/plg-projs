
-- DROP TABLE IF EXISTS fileinfo;
-- DROP TABLE IF EXISTS file_tags;

DROP TABLE IF EXISTS _info_projs_tags;
DROP TABLE IF EXISTS _info_projs_author_id;

-- CREATE TABLE IF NOT EXISTS _info_projs_tags (
    -- file TEXT NOT NULL,
    -- tag TEXT, 
    -- FOREIGN KEY(file) REFERENCES projs(file)
        -- ON DELETE CASCADE
        -- ON UPDATE CASCADE
-- );

-- CREATE TABLE IF NOT EXISTS _info_projs_author_id (
    -- file TEXT NOT NULL,
    -- author_id TEXT, 
    -- FOREIGN KEY(file) REFERENCES projs(file)
        -- ON DELETE CASCADE
        -- ON UPDATE CASCADE
-- );


